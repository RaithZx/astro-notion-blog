---
import '../styles/globals.css'
import { PUBLIC_GA_TRACKING_ID, ENABLE_LIGHTBOX } from '../server-constants.ts'
import { getDatabase } from '../lib/notion/client.ts'
import { getNavLink, getStaticFilePath, filePath } from '../lib/blog-helpers.ts'
import '../styles/syntax-coloring.css'
import GoogleAnalytics from '../components/GoogleAnalytics.astro'
import SearchModal from '../components/SearchModal.astro'

export interface Props {
  title: string
  description: string
  path: string
  ogImage: string
}

const { title = 'Ligadu', description = 'Artigus di siensia, teknolojia i saúdi', path = '/', ogImage = '' } = Astro.props

const database = await getDatabase()

const siteTitle = title ? `${title}` : 'Ligadu'
const siteDescription = description ? description : database.Description
const siteURL = new URL(getNavLink(path), Astro.site).toString()
const siteOGImage = new URL(
  getStaticFilePath('/ligadu_logo.png'),
  Astro.site
)

let coverImageURL: string
if (database.Cover) {
  if (database.Cover.Type === 'external') {
    coverImageURL = database.Cover.Url
  } else if (database.Cover.Type === 'file') {
    try {
      coverImageURL = filePath(new URL(database.Cover.Url))
    } catch {
      console.log('Invalid DB cover image URL: ', database.Cover?.Url)
    }
  }
}

let customIconURL: string = ''
if (database.Icon && database.Icon.Type === 'file') {
  try {
    customIconURL = filePath(new URL((database.Icon as any).Url))
  } catch {
    console.log('Invalid DB custom icon URL: ', (database.Icon as any)?.Url)
  }
}
---

<!DOCTYPE html>
<html lang="en" prefix="og: https://ogp.me/ns#">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="max-image-preview:large" />
    <meta charset="UTF-8" />
    <meta name="generator" content={Astro.generator} />
    <title>{siteTitle}</title>
    <meta name="description" content={siteDescription} />
    <link rel="canonical" href={siteURL} />
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/ligadu_logo.png" />
    <link rel="shortcut icon" href="/ligadu_logo.png" />
    <link rel="apple-touch-icon" href="/ligadu_logo.png" />
    <meta property="og:url" content={siteURL} />
    <meta property="og:title" content={siteTitle} />
    <meta property="og:description" content={siteDescription} />
    <meta property="og:site_name" content={database.Title} />
    <meta property="og:image" content={ogImage || siteOGImage} />
    <meta name="twitter:title" content={siteTitle} />
    <meta name="twitter:description" content={siteDescription} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImage || siteOGImage} />
    <!-- Google Fonts: Inter for body/UI, Space Grotesk for headlines -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&family=Space+Grotesk:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/katex@0.16.4/dist/katex.min.css"
      integrity="sha384-vKruj+a13U8yHIkAyGgK1J3ArTLzrFGBbBc0tDp4ad/EyewESeXE/Iv67Aj8gKZ0"
      crossorigin="anonymous"
    />
	 <script defer src="https://stats.ligadu.com/script.js" data-website-id="3a333f58-6b50-445d-9ccf-3cec581bc5e9"></script>
  </head>
  <body>
    <GoogleAnalytics trackingId={PUBLIC_GA_TRACKING_ID} />
    
    <!-- Sticky Navigation Header -->
    <header id="main-header" class="sticky top-0 z-50 border-b border-slate-200">
      <div class="px-5 max-sm:px-2.5 md:px-[3.25rem]">
        <div class="flex items-center justify-between py-3">
          <!-- Left Side: Mobile Menu + Logo -->
          <div class="flex items-center gap-3 md:hidden">
            <!-- Mobile Menu Button -->
            <button 
              id="mobile-menu-button" 
              class="bg-transparent border-0 text-slate-600 text-2xl cursor-pointer p-2 hover:bg-slate-100 rounded"
              aria-label="Toggle mobile menu"
            >
              ☰
            </button>
            <!-- Logo/Text -->
            <a href={getNavLink('/')} class="text-slate-700 no-underline text-lg font-medium hover:text-slate-900 transition-colors">
              Ligadu
            </a>
          </div>
          
          <!-- Desktop Navigation -->
          <nav class="hidden md:flex items-center gap-1">
            <a href={getNavLink('/')} class="text-slate-600 no-underline py-2 pl-0 pr-4 font-medium border-b-2 border-transparent hover:text-slate-900 hover:border-slate-400 transition-colors">Inisiu</a>
            <a href="#" class="text-slate-600 no-underline py-2 px-4 font-medium border-b-2 border-transparent hover:text-slate-900 hover:border-slate-400 transition-colors">Saúdi</a>
            <a href="#" class="text-slate-600 no-underline py-2 px-4 font-medium border-b-2 border-transparent hover:text-slate-900 hover:border-slate-400 transition-colors">Siénsia</a>
            <a href="#" class="text-slate-600 no-underline py-2 px-4 font-medium border-b-2 border-transparent hover:text-slate-900 hover:border-slate-400 transition-colors">Tekinolojia</a>
            <a href={getNavLink('/kontaktu')} class="text-slate-600 no-underline py-2 px-4 font-medium border-b-2 border-transparent hover:text-slate-900 hover:border-slate-400 transition-colors">Kontaktu</a>
          </nav>
          
          <!-- Right Side Actions -->
          <div class="flex items-center gap-3">
            <!-- Search Button (All screens) -->
            <button 
              class="open-search-modal bg-transparent border-0 text-slate-600 cursor-pointer p-2 hover:bg-slate-100 rounded transition-colors"
              aria-label="Search"
              type="button"
            >
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
              </svg>
            </button>
            
            <!-- Subscribe Button (Desktop only) -->
            <button class="hidden md:block newsletter-btn bg-transparent border border-slate-300 text-slate-700 px-3 py-1.5 rounded text-sm font-normal cursor-pointer hover:bg-slate-50 hover:border-slate-400 transition-colors">
              Inskreve
            </button>
          </div>
        </div>
      </div>
      
      <!-- Mobile Navigation Drawer -->
      <div id="mobile-drawer" class="md:hidden fixed inset-0 z-50 transform -translate-x-full transition-transform duration-300 ease-in-out">
        <!-- Overlay -->
        <div id="mobile-overlay" class="fixed inset-0 bg-black bg-opacity-50"></div>
        
        <!-- Drawer Content -->
        <div class="relative flex flex-col w-80 max-w-sm h-full bg-white shadow-xl">
          <!-- Drawer Header -->
          <div class="flex items-center justify-end p-4 border-b border-slate-200">
            <button 
              id="mobile-close-button" 
              class="bg-transparent border-0 text-slate-600 text-2xl cursor-pointer p-2 hover:bg-slate-100 rounded"
              aria-label="Close mobile menu"
            >
              ✕
            </button>
          </div>
          
          <!-- Drawer Navigation -->
          <div class="flex-1 p-4">
            <nav class="space-y-2">
              <a href={getNavLink('/')} class="block text-slate-600 no-underline py-3 px-4 font-medium rounded hover:text-slate-800 hover:bg-slate-100">Inisiu</a>
              <a href="#" class="block text-slate-600 no-underline py-3 px-4 font-medium rounded hover:text-slate-800 hover:bg-slate-100">Saúdi</a>
              <a href="#" class="block text-slate-600 no-underline py-3 px-4 font-medium rounded hover:text-slate-800 hover:bg-slate-100">Siénsia</a>
              <a href="#" class="block text-slate-600 no-underline py-3 px-4 font-medium rounded hover:text-slate-800 hover:bg-slate-100">Tekinolojia</a>
              <a href={getNavLink('/kontaktu')} class="block text-slate-600 no-underline py-3 px-4 font-medium rounded hover:text-slate-800 hover:bg-slate-100">Kontaktu</a>
            </nav>
            
            <!-- Mobile Search and CTA Buttons -->
            <div class="mt-6 pt-6 border-t border-slate-200 space-y-3">
              <button 
                class="open-search-modal w-full bg-transparent border border-slate-300 text-slate-700 px-4 py-2 rounded text-sm font-normal cursor-pointer hover:bg-slate-50 flex items-center justify-center gap-2"
                type="button"
              >
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                </svg>
                <span>Faze piskiza...</span>
              </button>
              <button class="newsletter-btn w-full bg-transparent border border-slate-300 text-slate-700 px-4 py-2 rounded text-sm font-normal cursor-pointer hover:bg-slate-50">
                Inskreve
              </button>
            </div>
          </div>
        </div>
      </div>
      
    </header>
      
      <!-- Advertisement Banner -->
      <!-- <div class="bg-blue-600 text-white py-5">
        <div class="max-w-6xl mx-auto flex items-center justify-between px-5">
          <div class="flex items-center gap-5">
            <div class="font-bold text-xl">AppOmni</div>
            <div class="ad-text">
              <h3 class="m-0 text-lg font-medium">Vishing crews like UNC6040 use rogue apps to breach Salesforce.</h3>
            </div>
          </div>
          <div class="flex items-center gap-4">
            <div class="text-3xl">📄</div>
            <button class="bg-white text-blue-600 border-0 px-5 py-2.5 rounded font-bold cursor-pointer hover:bg-slate-50">See the Controls</button>
          </div>
        </div>
      </div> -->
    </header>

    <div class="min-h-screen bg-slate-50">
      <div class="flex gap-8 px-5 py-8 max-sm:flex-col max-sm:gap-5 max-sm:px-2.5 max-sm:py-5">
        <!-- <main class="flex-[2] p-8  max-sm:p-5"> -->
        <main class="flex-[2]  p-8  max-sm:p-5">
          <slot name="main" />
        </main>

        <aside class="flex-1  p-8  h-fit max-sm:p-5">
          <!-- <SearchButton /> -->
          <slot name="aside" />
        </aside>
      </div>
    </div>

    <SearchModal />
    
    <!-- Footer -->
    <footer class="bg-slate-800 text-white mt-16">
      <div class="px-5 max-sm:px-2.5 py-12">
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
          <!-- Brand Section -->
          <div class="lg:col-span-1">
              <div class="flex items-center space-x-2 mb-4">
                <img src={getStaticFilePath('/ligadu_logo.png')} alt="Ligadu Logo" class="w-32 h-16" />
                <!-- <h3 class="text-xl font-bold">Ligadu</h3> -->
              </div>
            <p class="text-slate-300 text-sm mb-4">
              Artigus di siensia, teknolojia i saúdi.
            </p>
            <div class="flex space-x-4">
              <a href="#" class="text-slate-400 hover:text-white transition-colors" aria-label="Facebook">
                <span class="text-xl">📘</span>
              </a>
              <a href="#" class="text-slate-400 hover:text-white transition-colors" aria-label="Twitter">
                <span class="text-xl">🐦</span>
              </a>
              <a href="#" class="text-slate-400 hover:text-white transition-colors" aria-label="Instagram">
                <span class="text-xl">📷</span>
              </a>
              <a href="#" class="text-slate-400 hover:text-white transition-colors" aria-label="LinkedIn">
                <span class="text-xl">💼</span>
              </a>
              <a href="#" class="text-slate-400 hover:text-white transition-colors" aria-label="YouTube">
                <span class="text-xl">📺</span>
              </a>
            </div>
          </div>
          
          <!-- Quick Links -->
          <div>
            <h4 class="text-lg font-semibold mb-4">Links Rápidos</h4>
            <ul class="space-y-2">
              <li><a href={getNavLink('/')} class="text-slate-300 hover:text-white transition-colors text-sm">Inisiu</a></li>
              <li><a href="#" class="text-slate-300 hover:text-white transition-colors text-sm">Saúdi</a></li>
              <li><a href="#" class="text-slate-300 hover:text-white transition-colors text-sm">Siénsia</a></li>
              <li><a href="#" class="text-slate-300 hover:text-white transition-colors text-sm">Tekinolojia</a></li>
              <li><a href={getNavLink('/kontaktu')} class="text-slate-300 hover:text-white transition-colors text-sm">Kontaktu</a></li>
            </ul>
          </div>
          
          <!-- Legal Links -->
          <div>
            <h4 class="text-lg font-semibold mb-4">Legal</h4>
            <ul class="space-y-2">
              <li><a href="/privacidade" class="text-slate-300 hover:text-white transition-colors text-sm">Polítika di Privasidade</a></li>
              <li><a href="/termus" class="text-slate-300 hover:text-white transition-colors text-sm">Termus i Kondições</a></li>
              <li><a href="/kontaktu" class="text-slate-300 hover:text-white transition-colors text-sm">Kontaktu</a></li>
              <li><a href="#" class="text-slate-300 hover:text-white transition-colors text-sm">Cookies</a></li>
            </ul>
          </div>
          
          <!-- Newsletter & Contact -->
          <div>
            <h4 class="text-lg font-semibold mb-4">Fika ligadu</h4>
            <p class="text-slate-300 text-sm mb-4">
              Resebe kes ultimu artigus i notísias sobri siénsia, teknolojia i saúdi.
            </p>
            <button 
              id="footer-newsletter-btn"
              class="newsletter-btn w-full bg-transparent border border-slate-400 text-slate-200 px-4 py-2 rounded text-sm font-normal cursor-pointer hover:bg-slate-700 hover:border-slate-300 transition-colors"
            >
              Inskreve na Newsletter
            </button>
            
            <div class="mt-6 pt-6 border-t border-slate-700">
              <h5 class="font-semibold mb-2 text-sm text-slate-100">Kontaktu</h5>
              <div class="space-y-1 text-sm text-slate-100">
                <p>📧 kontaktu@ligadu.com</p>
                <p>🌐 www.ligadu.com</p>
                <p>📍 Cabo Verde</p>
              </div>
            </div>
          </div>
        </div>
        
        <!-- Bottom Bar -->
        <div class="border-t border-slate-700 mt-8 pt-8">
          <div class="flex flex-col md:flex-row justify-between items-center space-y-4 md:space-y-0">
            <div class="text-sm text-slate-400">
              © {new Date().getFullYear()} {database.Title}. Todu direitu reservadu.
            </div>
            <div class="flex items-center space-x-6 text-sm text-slate-400">
              <a href="#" class="hover:text-white transition-colors">🇨🇻</a>
            </div>
          </div>
        </div>
      </div>
    </footer>
    
    <!-- Newsletter Signup Modal -->
    <div id="newsletter-modal" class="fixed inset-0 z-50 hidden">
      <!-- Overlay -->
      <div id="newsletter-overlay" class="fixed inset-0 bg-black bg-opacity-50"></div>
      
      <!-- Modal Content -->
      <div class="relative flex items-center justify-center min-h-screen p-4">
        <div class="relative bg-white rounded-lg shadow-xl max-w-md w-full max-h-[90vh] overflow-y-auto">
          <!-- Modal Header -->
          <div class="flex items-center justify-between p-6 border-b border-slate-200">
            <h2 class="text-2xl font-bold text-slate-800">Inskreve na Newsletter</h2>
            <button 
              id="newsletter-close-button" 
              class="bg-transparent border-0 text-slate-600 text-2xl cursor-pointer p-2 hover:bg-slate-100 rounded"
              aria-label="Close newsletter modal"
            >
              ✕
            </button>
          </div>
          
          <!-- Modal Body -->
          <div class="p-6">
            <p class="text-slate-600 mb-6">
              Resebe kes ultimu artigus i notísias sobri siénsia, teknolojia i saúdi. 
              Inskreve i fika atualizadu!
            </p>
            
            <form id="newsletter-form" class="space-y-4">
              <div>
                <label for="newsletter-email" class="block text-sm font-medium text-slate-700 mb-2">
                  Email
                </label>
                <input 
                  type="email" 
                  id="newsletter-email" 
                  name="email"
                  required
                  class="w-full px-4 py-3 border border-slate-300 rounded-lg text-base focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-transparent"
                  placeholder="seu@email.com"
                />
              </div>
              
              <div>
                <label for="newsletter-name" class="block text-sm font-medium text-slate-700 mb-2">
                  Nome (opcional)
                </label>
                <input 
                  type="text" 
                  id="newsletter-name" 
                  name="name"
                  class="w-full px-4 py-3 border border-slate-300 rounded-lg text-base focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-transparent"
                  placeholder="Seu nome"
                />
              </div>
              
              <div class="flex items-start">
                <input 
                  type="checkbox" 
                  id="newsletter-privacy" 
                  name="privacy"
                  required
                  class="mt-1 h-4 w-4 text-amber-600 focus:ring-amber-500 border-slate-300 rounded"
                />
                <label for="newsletter-privacy" class="ml-2 text-sm text-slate-600">
                  Konkordo ku a <a href="/privacidade" class="text-amber-600 hover:text-amber-700 underline">polítika di privasidade</a> i 
                  <a href="/termus" class="text-amber-600 hover:text-amber-700 underline">termus di servisu</a>
                </label>
              </div>
              
              <button 
                type="submit" 
                class="w-full bg-amber-400 text-slate-900 border-0 px-6 py-3 rounded-lg font-bold cursor-pointer flex items-center justify-center gap-2 hover:bg-amber-500 transition-colors"
              >
                <span class="text-lg">✉</span>
                Inskreve na Newsletter
              </button>
            </form>
            
            <p class="text-xs text-slate-500 mt-4 text-center">
              Pode kansa kada momentu. Nunka spam, só kontenidu di kalidade.
            </p>
          </div>
        </div>
      </div>
    </div>
    
    {
      ENABLE_LIGHTBOX && (
        <script src={getStaticFilePath('/scripts/fslightbox.js')} />
      )
    }
    
    <!-- Mobile Menu & Newsletter Modal JavaScript -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        // Mobile Menu Elements
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const mobileCloseButton = document.getElementById('mobile-close-button');
        const mobileDrawer = document.getElementById('mobile-drawer');
        const mobileOverlay = document.getElementById('mobile-overlay');
        
        // Newsletter Modal Elements
        const newsletterModal = document.getElementById('newsletter-modal');
        const newsletterOverlay = document.getElementById('newsletter-overlay');
        const newsletterCloseButton = document.getElementById('newsletter-close-button');
        const newsletterForm = document.getElementById('newsletter-form');
        const ctaButtons = document.querySelectorAll('.newsletter-btn');
        const footerNewsletterBtn = document.getElementById('footer-newsletter-btn');
        const mainHeader = document.getElementById('main-header');
        
        // Mobile Menu Functions
        function openMobileMenu() {
          mobileDrawer.classList.remove('-translate-x-full');
          document.body.style.overflow = 'hidden';
        }
        
        function closeMobileMenu() {
          mobileDrawer.classList.add('-translate-x-full');
          document.body.style.overflow = '';
        }
        
        // Newsletter Modal Functions
        function openNewsletterModal() {
          newsletterModal.classList.remove('hidden');
          document.body.style.overflow = 'hidden';
          // Focus on email input
          setTimeout(() => {
            document.getElementById('newsletter-email').focus();
          }, 100);
        }
        
        function closeNewsletterModal() {
          newsletterModal.classList.add('hidden');
          document.body.style.overflow = '';
          // Reset form
          newsletterForm.reset();
        }
        
        // Mobile Menu Event Listeners
        mobileMenuButton.addEventListener('click', openMobileMenu);
        mobileCloseButton.addEventListener('click', closeMobileMenu);
        mobileOverlay.addEventListener('click', closeMobileMenu);
        
        // Newsletter Modal Event Listeners
        ctaButtons.forEach(button => {
          button.addEventListener('click', function(e) {
            e.preventDefault();
            openNewsletterModal();
          });
        });
        
        // Footer newsletter button
        if (footerNewsletterBtn) {
          footerNewsletterBtn.addEventListener('click', function(e) {
            e.preventDefault();
            openNewsletterModal();
          });
        }
        
        newsletterCloseButton.addEventListener('click', closeNewsletterModal);
        newsletterOverlay.addEventListener('click', closeNewsletterModal);
        
        // Newsletter Form Submission
        newsletterForm.addEventListener('submit', function(e) {
          e.preventDefault();
          
          const email = document.getElementById('newsletter-email').value;
          const name = document.getElementById('newsletter-name').value;
          const privacy = document.getElementById('newsletter-privacy').checked;
          
          if (!privacy) {
            alert('Por favor, konkordo ku a polítika di privasidade i termus di servisu.');
            return;
          }
          
          // Here you would typically send the data to your newsletter service
          // For now, we'll just show a success message
          alert(`Obrigadu! ${name ? name + ', ' : ''}inskrevu ku suksesu na newsletter.`);
          closeNewsletterModal();
        });
        
        // Close modals on escape key
        document.addEventListener('keydown', function(e) {
          if (e.key === 'Escape') {
            if (!mobileDrawer.classList.contains('-translate-x-full')) {
              closeMobileMenu();
            }
            if (!newsletterModal.classList.contains('hidden')) {
              closeNewsletterModal();
            }
          }
        });
        
        // Close mobile menu on window resize to desktop
        window.addEventListener('resize', function() {
          if (window.innerWidth >= 768) {
            closeMobileMenu();
          }
        });
        
        // Sticky header scroll effect with smooth transition
        let ticking = false;
        window.addEventListener('scroll', function() {
          if (!ticking) {
            window.requestAnimationFrame(function() {
              const scrollTop = window.pageYOffset || document.documentElement.scrollTop;
              
              if (scrollTop > 10) {
                mainHeader.classList.add('scrolled');
              } else {
                mainHeader.classList.remove('scrolled');
              }
              
              ticking = false;
            });
            ticking = true;
          }
        });
      });
    </script>
  </body>
</html>


<style is:global>
  :root {
    --gap-quarter: 0.25rem;
    --gap-half: 0.5rem;
    --gap: 1rem;
    --gap-double: 2rem;

    --bg: #f7fafc;
    --fg: #2d3748;
    --accents-1: #4a5568;
    --accents-2: #718096;
    --accents-3: #a0aec0;
    --geist-foreground: #2d3748;
    --geist-success: #38a169;

    --radius: 6px;

    --font-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI',
      'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans',
      'Helvetica Neue', sans-serif;
    --font-display: 'Space Grotesk', 'Inter', system-ui, -apple-system,
      BlinkMacSystemFont, 'Segoe UI', sans-serif;
    --font-mono: SFMono-Regular, Menlo, Consolas, 'PT Mono', 'Liberation Mono',
      Courier, monospace;
  }

  * {
    box-sizing: border-box;
    word-break: break-word;
  }

  html,
  body {
    padding: 0;
    margin: 0;
    font-size: 17px;
  }

  body {
    min-height: 100vh;
    background: var(--bg);
    color: var(--fg);
    font-family: var(--font-sans);
    font-size: 17px;
    line-height: 1.7;

    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    -webkit-text-size-adjust: 100%;
  }

  figure {
    margin: 0;
    font-size: 0.85rem;
    color: #999;
    line-height: 1.8rem;
  }

  a {
    color: var(--accents-1);
    text-decoration: none;
  }
  a:hover {
    color: var(--accents-1);
  }

  mark {
    padding: var(--gap-quarter);
    border-radius: var(--radius);
    background: rgba(247, 212, 255, 0.8);
  }

  h1 {
    margin: 0;
    color: var(--fg);
    font-family: var(--font-display);
    font-size: 2rem;
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  h2 {
    margin: 0;
    color: var(--fg);
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    line-height: 1.3;
    letter-spacing: -0.01em;
  }

  h3 {
    margin: 0;
    color: var(--fg);
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 700;
    line-height: 1.4;
    letter-spacing: 0;
  }

  @media (max-width: 640px) {
    h1 {
      font-size: 1.6rem;
    }
    h2 {
      font-size: 1.3rem;
    }
    h3 {
      font-size: 1.1rem;
    }
  }

  ol {
    color: #222;
    font-weight: 400;
    font-size: 0.9rem;
    line-height: 1.7;
  }
  p,
  ul {
    color: #222;
    font-weight: 400;
    font-size: 1rem;
    line-height: 1.7;
  }
  ul,
  ol {
    margin: 0;
    padding-inline-start: 1.5rem;
  }
  pre {
    margin: 0;
    white-space: pre;
    tab-size: 2;
  }
  pre :global(code) {
    overflow: auto;
    -webkit-overflow-scrolling: touch;
  }

  hr {
    display: block;
    height: 1px;
    border: 0;
    margin: 0.3rem 0;
    background-color: #333;
  }

  code {
    font-size: 0.9rem;
    background: rgba(135, 131, 120, 0.15);
    color: #eb5757;
    padding: 0.25rem;
    border-radius: var(--radius);
    font-family: var(--font-mono);
  }

  table {
    display: block;
    overflow-x: auto;
    white-space: nowrap;
    border-collapse: collapse;
  }

  table tr {
    vertical-align: top;
  }

  table th {
    font-weight: 600;
    background-color: #f4f4f4;
  }

  table td,
  table th {
    font-size: 0.9rem;
    border: 1px solid #e4e4e4;
    padding: 0.5rem 1rem;
    text-align: left;
    line-height: 1.2rem;
  }
  table td::after {
    content: '';
    display: inline-block;
    min-height: 0.9rem;
  }
  
  /* Mobile Responsive Styles */
  @media (max-width: 767px) {
    .header-center h1 {
      font-size: 1.5rem;
    }
    
    .custom-icon {
      width: 1.5rem;
      height: 1.5rem;
    }
  }
  
  /* Mobile Drawer Styles */
  #mobile-drawer {
    z-index: 9999;
  }
  
  #mobile-drawer.show {
    transform: translateX(0);
  }
  
  /* Smooth transitions for mobile menu */
  .mobile-menu-transition {
    transition: transform 0.3s ease-in-out;
  }
  
  /* Prevent body scroll when mobile menu is open */
  body.mobile-menu-open {
    overflow: hidden;
  }
  
  /* Responsive main content */
  @media (max-width: 640px) {
    .max-w-6xl {
      padding-left: 1rem;
      padding-right: 1rem;
    }
    
    .flex.max-w-6xl.mx-auto.gap-8.px-5.py-8 {
      flex-direction: column;
      gap: 1.25rem;
      padding: 1.25rem 1rem;
    }
    
    main.flex-\[2\] {
      padding: 1.25rem;
    }
    
    aside.flex-1 {
      padding: 1.25rem;
    }
  }
  
  /* Newsletter Modal Styles */
  #newsletter-modal {
    backdrop-filter: blur(4px);
  }
  
  #newsletter-modal .relative {
    animation: modalSlideIn 0.3s ease-out;
  }
  
  @keyframes modalSlideIn {
    from {
      opacity: 0;
      transform: translateY(-20px) scale(0.95);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }
  
  /* Form input focus styles */
  #newsletter-form input:focus {
    box-shadow: 0 0 0 3px rgba(251, 191, 36, 0.1);
  }
  
  /* Checkbox styling */
  #newsletter-privacy:checked {
    background-color: #f59e0b;
    border-color: #f59e0b;
  }
  
  /* Button hover effects */
  #newsletter-form button:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(251, 191, 36, 0.3);
  }
  
  /* Mobile modal adjustments */
  @media (max-width: 640px) {
    #newsletter-modal .relative {
      padding: 1rem;
    }
    
    #newsletter-modal .max-w-md {
      max-width: 100%;
    }
  }
  
  /* Footer Styles */
  footer {
    margin-top: auto;
  }
  
  footer a:hover {
    transform: translateY(-1px);
  }
  
  footer .social-links a {
    transition: all 0.3s ease;
  }
  
  footer .social-links a:hover {
    transform: scale(1.1);
  }
  
  /* Footer responsive adjustments */
  @media (max-width: 768px) {
    footer .grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    
    footer .lg\\:col-span-1 {
      text-align: center;
    }
    
    footer .flex.space-x-4 {
      justify-content: center;
    }
  }
  
  @media (max-width: 640px) {
    footer {
      padding: 2rem 1rem;
    }
    
    footer .max-w-6xl {
      padding: 0;
    }
  }
  
  /* Sticky Header Scroll Effect */
  #main-header {
    background-color: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(5px);
    box-shadow: 0 0 0 rgba(0, 0, 0, 0);
    transition: background-color 0.2s ease-out,
                box-shadow 0.2s ease-out,
                backdrop-filter 0.2s ease-out;
  }
  
  #main-header.scrolled {
    background-color: rgba(255, 255, 255, 1);
    backdrop-filter: blur(20px);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
  }
  
  #main-header.scrolled .border-b {
    border-color: rgba(226, 232, 240, 0.9);
  }
</style>
