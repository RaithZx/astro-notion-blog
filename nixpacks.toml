[phases.setup]
nixPkgs = ["...", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node ./node_modules/.bin/astro preview --host 0.0.0.0 --port ${PORT:-4321}"
